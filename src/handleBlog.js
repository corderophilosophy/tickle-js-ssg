const fs = require('fs-extra');

module.exports = function(blogDir) {
  console.log(blogDir);
  // const blog = fs.readdirSync(blogDir);
  // for (let post in blog) {
  //   console.log(post);
  // }
};
